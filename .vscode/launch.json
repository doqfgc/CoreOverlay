{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch CoreOverlay",
            "type": "firefox",
            "request": "launch",
            "reAttach": true,
            "file": "${workspaceFolder}/main.html"
        }
    ]
}